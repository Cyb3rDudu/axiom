# Python
__pycache__/
*.pyc
# Bare `.venv` (no trailing slash) also catches symlinks — worktrees link
# a shared venv via `.venv` -> ../.venv and must never commit the link.
.venv
.venv/
.venv-docs/
.pytest_cache/

# MkDocs build output
site/

# Go
*.test

# Node-Reste (falls je wieder)
node_modules/

# Betrieb & lokale Daten
artifacts/
backups/
docker-logs.txt
*.log

# Tools
.pi/
.claude/
.idea/
.vscode/
.DS_Store
sessions/

# Deployment staging (#205)
dist/
axiom_ng_runner.egg-info/
axiom_ng_runner/build/
