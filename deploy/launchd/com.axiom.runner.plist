<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- com.axiom.runner — query/ingest runner (#205 §3).
     Template: copy to ~/Library/LaunchAgents/ after install.
     Secrets/env live in ~/.config/axiom/runner.env (0700) — never in /tmp.
     Expected env vars (runner.env): AXIOM_PROCESSOR_COMPUTE=real,
     AXIOM_PROCESSOR_PORT=8012, plus any AXIOM_PROCESSOR_* auth settings.
     NOTE: exec via `env/bin/python -m axiom_ng_runner`, NOT the
     env/bin/axiom-runner console script — conda-unpack does not rewrite pip
     console-script shebangs (#!/usr/bin/env python), and launchd's PATH has
     no python. See deploy/launchd/README.md. -->
<plist version="1.0">
<dict>
    <key>Label</key><string>com.axiom.runner</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/sh</string>
        <string>-c</string>
        <string>. "$HOME/.config/axiom/runner.env" &amp;&amp; exec /opt/axiom/runner/current/env/bin/python -m axiom_ng_runner</string>
    </array>
    <key>RunAtLoad</key><true/>
    <key>KeepAlive</key><true/>
    <key>ProcessType</key><string>Background</string>
    <key>StandardOutPath</key><string>__HOME__/.local/state/axiom/logs/runner.log</string>
    <key>StandardErrorPath</key><string>__HOME__/.local/state/axiom/logs/runner.log</string>
</dict>
</plist>
