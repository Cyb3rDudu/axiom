<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- com.axiom.carrier-bridge — TEMPLATE ONLY (#205 §3).
     The Apple-python bridge that publishes local runner ports on
     127.0.0.1:19542-19544 so TCC-blocked unsigned binaries remain
     reachable from the LAN (2026-08-23 night workaround).
     May retire once binaries are TCC-granted — keep as template.
     ProgramArguments is a PLACEHOLDER: point __BRIDGE_CMD__ at the
     concrete bridge invocation; env in ~/.config/axiom/carrier-bridge.env.
     __HOME__ substituted by scripts/install_services.sh. -->
<plist version="1.0">
<dict>
    <key>Label</key><string>com.axiom.carrier-bridge</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/sh</string>
        <string>-c</string>
        <string>. "$HOME/.config/axiom/carrier-bridge.env" &amp;&amp; exec __BRIDGE_CMD__</string>
    </array>
    <key>RunAtLoad</key><true/>
    <key>KeepAlive</key><true/>
    <key>ProcessType</key><string>Background</string>
    <key>StandardOutPath</key><string>__HOME__/.local/state/axiom/logs/carrier-bridge.log</string>
    <key>StandardErrorPath</key><string>__HOME__/.local/state/axiom/logs/carrier-bridge.log</string>
</dict>
</plist>
