# axiom_backend (legacy)

**Status: dead tree.** The compute modules (chunker, embedder, entity +
relation extractors, the Marker processing path, pdf/epub workers, device
selection) moved to `axiom_ng_runner/compute_core` (#118, git-mv — history
preserved there). Imports in this tree are broken by design; nothing here
runs. The tree awaits the archive-and-shrink in #117's closure.
