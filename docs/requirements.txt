# Python deps for building the axiom documentation site.
# Pinned to the versions validated locally (Python 3.9) and in CI
# (Python 3.11) — mkdocs 1.6.1 supports both.
mkdocs==1.6.1
mkdocs-material==9.7.7
