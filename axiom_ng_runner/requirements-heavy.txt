# axiom_ng_runner — heavy-compute dependencies for the REAL backend (Gate 5).
# Install ON TOP of requirements.txt (the reference/lightweight deps) when running
# AXIOM_PROCESSOR_COMPUTE=real. These pull torch, transformers, and ML models.
#
# Verified versions (fresh nix-python311 venv, Apple MPS available: True):
# marker-pdf==1.10.2  — last v1 before the v2 API jump (proven in ~/Code/marker-poc)
# torch 2.13.0 with surya-ocr 0.17.1 — Apple GPU acceleration via torch.backends.mps
#
# The vendored compute_core pulls no DB driver — the runner is compute-only,
# no sqlalchemy/psycopg entry is needed anymore.

# PDF conversion (Marker v1).
marker-pdf==1.10.2

# Dense/sparse embeddings (BGE-M3).
FlagEmbedding==1.4.0

# Entity extraction (GLiNER).
gliner==0.2.28

# mrebel (relation extraction) loads lazily via transformers AutoModelForSeq2SeqLM
# at runtime — no explicit package beyond what marker-pdf/transformers pulls.
